version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: peluqueria-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-peluquero}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-peluqueria123}
      POSTGRES_DB: ${DB_NAME:-peluqueria}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-peluquero} -d ${DB_NAME:-peluqueria}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
